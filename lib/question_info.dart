class QuestionInfo  {
  String questionImage;
  String questionText;
  bool  questionAnswer;
  QuestionInfo({required this.questionImage,required this.questionText,required this.questionAnswer}){
    this.questionAnswer=questionAnswer;
    this.questionText=questionText;
    this.questionImage=questionImage;
  }
}